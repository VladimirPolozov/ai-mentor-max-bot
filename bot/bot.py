import aiomax
from config import MAX_BOT_TOKEN

def create_bot() -> aiomax.Bot:
    bot = aiomax.Bot(MAX_BOT_TOKEN, default_format="markdown")

    @bot.on_bot_start()
    async def send_welcome(payload: aiomax.BotStartPayload):
        await payload.send(
            "👋 Привет! Я — ИИ-наставник для студентов направления «Прикладная информатика в экономике» (КемГУ).\n\nЭто MVP-версия: пока я отвечаю только на вопросы по вашему направлению, но функциональность будет постепенно расширяться! Задавай мне любые вопросы!"
        )

    # Позже сюда добавятся @bot.on_message() и другие хендлеры
    return bot
